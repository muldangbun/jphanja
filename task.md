# Story Mode Grammar Modal Implementation

- [x] Update `kaninotokoya.JSON` with simplified grammar data structure for the first sentence.
- [x] Add CSS for the Glassmorphism Grammar Modal in `index.css`.
- [x] Add the `💡 문법 해설` button to `index.html` within the sentence view.
- [x] Implement the Grammar Modal HTML structure in `index.html`.
- [x] Add JavaScript logic to populate and toggle the Grammar Modal based on the current sentence's data.
- [x] Test the modal behavior and ensure it looks good and fits the Zen style.
